version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./database.sqlite:/app/database.sqlite
    environment:
      - PORT=3000
      - NODE_ENV=production
      - SESSION_SECRET=your-very-secure-secret-here
      - ADMIN_KEY=ken123
    restart: always
